'use client';

import { ReactNode } from 'react';

export default function AdminManageLayout({ children }: { children: ReactNode }) {
    return (
        <>
            {children}
        </>
    );
}
